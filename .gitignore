# Secrets — never commit
.env
.env.*
!.env.example

# macOS
.DS_Store

# Maps and scripts (local tooling, optional to keep)
*.pyc
__pycache__/
.vercel

# Next.js
/node_modules
/.next/
/out/
/build
*.tsbuildinfo
next-env.d.ts

# Legacy static-export files no longer used to build the site (see
# MARANG-HOUSE-SITE-CONTEXT.md §10). Not deployed (see .vercelignore).
# The bulky local image dumps and the 6.6MB bundled export are excluded
# from git entirely — the images already live on Cloudinary, so these
# are pure local backup clutter. Everything else in archive/ is small
# text/code kept for historical reference.
archive/uploads/
archive/web-images/
archive/screenshots/
archive/cloudinary_upload/
archive/Marang House (standalone).html

# Canva redesign (branch redesign/canva-v2): local mirror of the Cloudinary
# folders + the 16 reference screenshots. Assets already live on Cloudinary
# (cloud name m4hqddxx); these are pure local reference, not build inputs.
cloudinary_upload2/
design-reference/

# Playwright MCP tooling output (QA screenshots/logs from this session, not
# site content).
.playwright-mcp/
